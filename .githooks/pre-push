#!/bin/sh

# Pre-push hook: run quality checks before push
echo "Running quality checks..."

make check

if [ $? -ne 0 ]; then
    echo "❌ Quality checks failed. Push aborted."
    exit 1
fi

echo "✅ Quality checks passed."
exit 0
