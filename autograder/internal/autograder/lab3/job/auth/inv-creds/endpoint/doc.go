// Package endpoint contains the implementation of the auth failed endpoint test step,
// which checks that the authentication endpoint correctly handles failed authentication attempts.
package endpoint
