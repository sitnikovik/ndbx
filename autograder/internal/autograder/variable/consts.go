package variable

const (
	// User is the key for the user document variable in the autograder.
	User = "user"
	// UserPassword is the key for the user password document variable in the autograder.
	UserPassword = "user_password"
	// SessionTTL is the key for the session TTL document variable in the autograder.
	SessionTTL = "session_ttl"
	// Session is the key for the session document variable in the autograder.
	Session = "session"
	// EventID is the key for the event ID variable in the autograder.
	EventID = "event_id"
)
